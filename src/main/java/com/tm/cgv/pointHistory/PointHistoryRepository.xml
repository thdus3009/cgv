<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tm.cgv.pointHistory.PointHistoryRepository">
	
	<insert id="pointHistortInsert" parameterType="PointHistoryVO">
		insert into pointHistory values(0,#{username},#{inputPrice},#{outputPrice},#{type},sysdate())
	</insert>
	
	<select id="pointHistoryList" parameterType="Pager" resultType="PointHistoryVO">
		select * from pointHistory
		where username like concat('%',#{search},'%')
		<if test="kind != ''">
			and type=#{kind}
		</if>
		<if test="startDate != ''">
			and #{startDate} <![CDATA[<=]]>  createAt
		</if>
		<if test="endDate != ''">
			and #{endDate} <![CDATA[>=]]>  createAt
		</if>
		order by num desc
		limit #{startRow},10
	</select>
	
	<select id="pointHistoryListCount" parameterType="Pager" resultType="Long">
		select count(num) from pointHistory
		where username like concat('%',#{search},'%')
		<if test="kind != ''">
			and type=#{kind}
		</if>
		<if test="startDate != ''">
			and #{startDate} <![CDATA[<=]]>  createAt
		</if>
		<if test="endDate != ''">
			and #{endDate} <![CDATA[>=]]>  createAt
		</if>
		order by num desc
		limit #{startRow},10
	</select>
	

</mapper>
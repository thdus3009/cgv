<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tm.cgv.cinema.CinemaRepository">
	
	<insert id="cinemaInsert" parameterType="CinemaVO">
		insert into cinema (name, local, address, tel, totalTheater, totalSeat, trafficInfo, parkingInfo, intro)
		values (#{name}, #{local}, #{address}, #{tel}, 0, 0, #{trafficInfo}, #{parkingInfo}, #{intro})
	</insert>

	<select id="cinemaSelect" parameterType="CinemaVO" resultType="CinemaVO">
		select * from cinema where num=#{num}
	</select>
	
	<select id="selectTheaterList" parameterType="Integer" resultType="TheaterVO">
		select * from theater where cinemaNum=#{num}
	</select>
	
	<select id="selectLocalCinemaNameList" parameterType="String" resultType="CinemaVO">
		select num,name from cinema where local=#{local};
	</select>
	
	
	<update id="cinemaUpdate" parameterType="CinemaVO">
		update cinema set name=#{name}, local=#{local}, address=#{address}, tel=#{tel}, totalTheater=#{totalTheater}, totalSeat=#{totalSeat},
		trafficInfo=#{trafficInfo}, parkingInfo=#{parkingInfo}, intro=#{intro} where num=#{num}
	</update>
	

	<update id="totalUpdate" parameterType="CinemaVO">
		update cinema set totalTheater=totalTheater+1, totalSeat=totalSeat+#{totalSeat} where num=#{num}
	</update>
	

	<delete id="cinemaDelete" parameterType="Integer">
		delete from cinema where num=#{num}
	</delete>
	
	
	<select id="cinemaLocalList" resultType="CinemaVO">
		select local from cinema group by local
	</select>
	
	<select id="cinemaList" resultType="CinemaVO">
		select * from cinema order by local
	</select>
	
	<select id="adminCinemaList" parameterType="Pager" resultType="CinemaVO">
		select * from cinema 		
		<where>
		<if test="local != ''">
			local=#{local}
		</if>
		</where>
		order by num desc limit #{startRow}, #{perPage}
	</select>
	
	<select id="adminCinemaCount" parameterType="Pager_cinema" resultType="Integer">
		select count(num) from cinema 		
		<where>
		<if test="local != ''">
			local=#{local}
		</if>
		</where>
	</select>
	
	<update id="theaterDelete" parameterType="TheaterVO">
		update cinema set totalTheater=totalTheater-1, totalSeat=totalSeat-#{seatCount} where num=#{cinemaNum}
	</update>
	
	

</mapper>